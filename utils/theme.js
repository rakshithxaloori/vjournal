import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
    },
    text: {
      primary: "#fff",
      secondary: "#bdbdbd",
    },
  },
});

export default theme;
