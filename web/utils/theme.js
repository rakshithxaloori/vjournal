import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
    },
    text: {
      primary: "#fff",
      secondary: "#bdbdbd",
    },
    background: {
      default: "#000",
      paper: "#424242",
    },
  },
});

export default theme;
